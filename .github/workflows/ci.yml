name: Integración continua

on:
  push:

jobs:
  ci:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up java
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      
      - name: Building
        run: ./gradlew build
      - name: Testing
        run: ./gradlew test
